#!/usr/bin/env perl
 
 use File::Basename;
 use lib $ENV{HOME}.'/workspace/mysql-cmds/ensembl/lib';
 use ScriptFunc;
 my @args = qw(localhost 3306 ensadmin);
 push @args, $ENV{ENSADMIN_PSW};
 run_mysql(@args);
